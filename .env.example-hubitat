# Hubitat Agent Configuration

# Method 1: Provide complete API URL
HUBITAT_API_URL=http://192.168.10.109/apps/api/50/devices/all?access_token=cab9d803-e74b-443d-a81c-bba57d8b274b

# Method 2: Provide host and token separately (alternative to HUBITAT_API_URL)
# HUBITAT_HOST=192.168.10.109
# HUBITAT_TOKEN=cab9d803-e74b-443d-a81c-bba57d8b274b

# TimescaleDB connection
TIMESCALEDB_URL=postgresql://sensor_user:sensor_password@localhost:5432/sensor_data

# Agent mode: 'poll' for continuous polling, 'server' for webhook listening
MODE=poll

# Poll interval (seconds) - only used in poll mode
POLL_INTERVAL_SECONDS=30

# Server mode settings (only if MODE=server)
HOST=0.0.0.0
PORT=8080

# Run once and exit (useful for testing)
RUN_ONCE=false
